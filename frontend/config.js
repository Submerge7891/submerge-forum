// 后端 API 基础地址
// 开发环境：使用相对路径（由后端 serve 前端时）
// 生产环境：如果需要单独部署前端，可以改为完整 URL
const API_BASE = ''; // 空字符串表示同域（由后端提供静态文件服务）

// 如果需要独立部署前端（比如用 GitHub Pages），可以取消下面这行的注释并修改为实际后端地址
// const API_BASE = 'http://localhost:3000'; // 或者你的服务器地址
